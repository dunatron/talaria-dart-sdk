name: talaria_flutter_example
description: Example app for talaria_flutter
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  talaria_flutter:
    path: ../

dependency_overrides:
  talaria:
    path: ../../talaria

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
